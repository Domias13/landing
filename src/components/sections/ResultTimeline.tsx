import type { Lang } from '@/lib/i18n';
import { t } from '@/lib/i18n';
import { Reveal } from '@/components/Reveal';

export function ResultTimeline({ lang }: { lang: Lang }) {
  const items = [
    {
      b: t(lang, 'Он начинает думать о тебе', 'Він починає думати про тебе'),
      s: t(lang, 'Тишина с твоей стороны ломает его фильм. Мозг сам достраивает: «а что там у неё?»', 'Тиша з твого боку ламає його фільм. Мозок сам добудовує: «а що там у неї?»'),
    },
    {
      b: t(lang, 'Проверяет твои соцсети', 'Перевіряє твої соцмережі'),
      s: t(lang, 'Да, они тоже это делают. С чужих аккаунтов. Молча.', 'Так, вони теж це роблять. З чужих акаунтів. Мовчки.'),
    },
    {
      b: t(lang, 'Говорит, что сделал самую большую ошибку в жизни', 'Каже, що зробив найбільшу помилку в житті'),
      s: t(lang, 'Сначала — другу за пивом. Потом — тебе.', 'Спочатку — другу за пивом. Потім — тобі.'),
    },
    {
      b: t(lang, 'Сам ищет повод написать или встретиться', 'Сам шукає привід написати чи зустрітись'),
      s: t(lang, '«Привет, как дела» в час ночи. И тут срабатывает день 6 — у тебя уже готов ответ.', '«Привіт, як справи» о першій ночі. І тут спрацьовує день 6 — у тебе вже готова відповідь.'),
    },
  ];

  return (
    <Reveal as="section">
      <div className="wrap">
        <span className="plate gld">{t(lang, 'Что происходит дальше', 'Що відбувається далі')}</span>
        <h2>{t(lang, 'Когда действуешь правильно — всё разворачивается', 'Коли дієш правильно — все розвертається')}</h2>
        <div className="tl">
          {items.map((item, i) => (
            <div className="tl-item" key={i}>
              <b>{item.b}</b>
              <span>{item.s}</span>
            </div>
          ))}
        </div>
      </div>
    </Reveal>
  );
}
