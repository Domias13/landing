import type { Lang } from '@/lib/i18n';
import { t } from '@/lib/i18n';
import { Reveal } from '@/components/Reveal';

export function NotFor({ lang }: { lang: Lang }) {
  return (
    <Reveal as="section" className="notfor">
      <div className="wrap">
        <span className="plate" style={{ background: '#3a3444' }}>
          {t(lang, 'Честно', 'Чесно')}
        </span>
        <h2>{t(lang, 'Кому интенсив НЕ подходит', 'Кому інтенсив НЕ підходить')}</h2>
        <div className="pain-list">
          <div className="pain">
            {t(
              lang,
              'Тем, кому нравится страдать красиво. Если боль стала способом получать внимание подруг — тебе туда, а не сюда.',
              'Тим, кому подобається страждати красиво. Якщо біль став способом отримувати увагу подруг — тобі туди, а не сюди.',
            )}
          </div>
          <div className="pain">
            {t(
              lang,
              'Тем, кто ждёт волшебное заклинание. Здесь на каждый день есть задание. Не сделаешь — не сработает.',
              'Тим, хто чекає чарівне заклинання. Тут кожен день є завдання. Не зробиш — не спрацює.',
            )}
          </div>
          <div className="pain">
            {t(
              lang,
              '«Спасательницам», готовым простить всё заранее. Сначала возвращают уважение к себе. Потом — его. Порядок не меняется.',
              '«Рятувальницям», готовим пробачити все заздалегідь. Спочатку повертають повагу до себе. Потім — його. Порядок не змінюється.',
            )}
          </div>
          <div className="pain">
            {t(
              lang,
              'Если в отношениях было насилие — тебе нужен не интенсив, а профильный специалист. Это серьёзно.',
              'Якщо у стосунках було насильство — тобі потрібен не інтенсив, а профільний спеціаліст. Це серйозно.',
            )}
          </div>
        </div>
        <p className="mut">
          {t(lang, 'Все остальные — ниже. Там тарифы.', 'Усі інші — нижче. Там тарифи.')}
        </p>
      </div>
    </Reveal>
  );
}
