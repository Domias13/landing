import { useState } from 'react';
import type { Lang } from '@/lib/i18n';
import { t } from '@/lib/i18n';
import { Reveal } from '@/components/Reveal';

function FaqItem({ q, a }: { q: string; a: string }) {
  const [open, setOpen] = useState(false);
  return (
    <details open={open} onClick={(e) => { e.preventDefault(); setOpen((v) => !v); }}>
      <summary>{q}</summary>
      <div className="a">{a}</div>
    </details>
  );
}

export function Faq({ lang }: { lang: Lang }) {
  const items = [
    {
      q: t(lang, 'А если он не вернётся?', 'А якщо він не повернеться?'),
      a: t(lang, 'День 7 закрывает оба сценария. Вернулся — ты знаешь, как действовать, чтобы не наступить на те же грабли. Не вернулся — ты выходишь из этой истории с прямой спиной, а не с годами терапии. В любом финале выигрываешь ты.', 'День 7 закриває обидва сценарії. Повернувся — ти знаєш, як діяти, щоб не наступити на ті самі граблі. Не повернувся — ти виходиш із цієї історії з прямою спиною, а не з роками терапії. У будь-якому фіналі виграєш ти.'),
    },
    {
      q: t(lang, 'А если я сорвусь на второй день?', 'А якщо я зірвусь на другий день?'),
      a: t(lang, 'Сорвёшься — и что? В интенсиве есть отдельный протокол на случай срыва: что делать в ближайший час, чтобы один слабый вечер не перечеркнул всё. Срыв — это не конец. Конец — это когда ты ничего не делаешь.', 'Зірвешся — і що? В інтенсиві є окремий протокол на випадок зриву: що робити в найближчу годину, щоб один слабкий вечір не перекреслив усе. Зрив — це не кінець. Кінець — це коли ти нічого не робиш.'),
    },
    {
      q: t(lang, 'Это манипуляции?', 'Це маніпуляції?'),
      a: t(lang, 'Это знание того, как он думает. Ты не заставляешь его ничего делать. Ты перестаёшь кормить сценарий, в котором ты — фон его новой жизни. Всё остальное он делает сам.', 'Це знання того, як він думає. Ти не змушуєш його нічого робити. Ти перестаєш годувати сценарій, у якому ти — фон його нового життя. Все інше він робить сам.'),
    },
    {
      q: t(lang, 'У меня нет сил вообще ни на что', 'У мене немає сил взагалі ні на що'),
      a: t(lang, 'Поэтому и аудио. Наушники, кровать, 15 минут. День 1 рассчитан на состояние, когда сил ноль и трясёт. С этой точки и начинаем.', 'Тому і аудіо. Навушники, ліжко, 15 хвилин. День 1 розрахований на стан, коли сил нуль і трясе. З цієї точки і починаємо.'),
    },
    {
      q: t(lang, 'Когда придёт доступ?', 'Коли прийде доступ?'),
      a: t(lang, 'Сразу после оплаты, на почту. Можешь начать день 1 через пять минут — сегодня ночью, если надо.', 'Одразу після оплати, на пошту. Можеш почати день 1 через п\'ять хвилин — сьогодні вночі, якщо треба.'),
    },
  ];

  return (
    <Reveal as="section">
      <div className="wrap">
        <h2>{t(lang, 'Вопросы, которые ты сейчас крутишь в голове', 'Питання, які ти зараз крутиш у голові')}</h2>
        {items.map((item, i) => (
          <FaqItem key={i} q={item.q} a={item.a} />
        ))}
      </div>
    </Reveal>
  );
}
